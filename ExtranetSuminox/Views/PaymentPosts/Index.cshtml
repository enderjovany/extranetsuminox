@model IEnumerable<ExtranetSuminox.Models.PaymentPost>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Payment)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Payment)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdPaymentPost }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdPaymentPost }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdPaymentPost })
        </td>
    </tr>
}

</table>
