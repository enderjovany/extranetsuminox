@model IEnumerable<ExtranetSuminox.Models.RangeIniPost>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.RangeIni)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.RangeIni)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdRangeIniPost }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdRangeIniPost }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdRangeIniPost })
        </td>
    </tr>
}

</table>
