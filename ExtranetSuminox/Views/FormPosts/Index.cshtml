@model IEnumerable<ExtranetSuminox.Models.FormPost>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Form)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Form)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdFormPost }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdFormPost }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdFormPost })
        </td>
    </tr>
}

</table>
