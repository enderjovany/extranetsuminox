@model IEnumerable<ExtranetSuminox.Models.ProviderTypePost>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ProviderType)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ProviderType)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdProviderTypePost }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdProviderTypePost }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdProviderTypePost })
        </td>
    </tr>
}

</table>
