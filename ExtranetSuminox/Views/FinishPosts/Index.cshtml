@model IEnumerable<ExtranetSuminox.Models.FinishPost>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Finish)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Finish)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdFinishPost }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdFinishPost }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdFinishPost })
        </td>
    </tr>
}

</table>
